#!/bin/sh -e

# Source debconf library.
. /usr/share/debconf/confmodule

set INSTALLER=/usr/share/exodus/exodus-installer@EXODUS_SUFFIX@
if [ -e $INSTALLER ]
then
	$INSTALLER install @EXODUS_VERSION@
fi

#DEBHELPER#

# vim: ts=4 sw=4 et
