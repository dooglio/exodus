#!/bin/sh -e

# Source debconf library.
. /usr/share/debconf/confmodule

if [ "$1" = "purge" ]
then
    set INSTALLER=/usr/share/exodus/exodus-installer@EXODUS_SUFFIX@
    if [ -e $INSTALLER ]
    then
        $INSTALLER cleanup @EXODUS_VERSION@
    fi
fi

#DEBHELPER#

# vim: ts=4 sw=4 et
